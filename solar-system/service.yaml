apiVersion: v1
kind: Service
metadata:
  name: solar-system-service
  namespace: solar-system  # Added
  labels:
    app: solar-system
spec:
  type: NodePort
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: solar-system
