kind: Service
metadata:
  labels:
    app: solar-system-app
  name: solar-system-service
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: solar-system-app
  type: NodePort
